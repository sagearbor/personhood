# pkg/proto

Wire-format definitions used across Personhood SDKs and the reference server. Will hold:

- W3C Verifiable Credential JSON-LD `@context` document for `https://personhood.protocol/credentials/v1`
- Personhood credential JSON Schema
- Policy DSL JSON Schema (YAML files validate against the same schema)
- Protobuf definitions for any binary wire formats (TBD)
