# pkg/types

Shared type definitions across Personhood: `PersonhoodCredential`, `MethodMetadata`, `Policy`, `EvaluationResult`, `NullifierBinding`. All other modules depend on these. No dependencies on other Personhood modules.
