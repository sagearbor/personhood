package types

// Canonical Personhood types. To be defined in subsequent PRs:
//   PersonhoodCredential — W3C VC carrying verified methods, anchor flag, nullifier binding
//   MethodMetadata       — type, strength, cost, ux_friction, platform_requirements, freshness_lifetime
//   Policy               — integrator-declared verification requirements
//   EvaluationResult     — pass/fail + structured error code + derived nullifier (if any)
//   NullifierBinding     — commitment to user secret enabling per-context nullifier derivation
