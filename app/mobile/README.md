# app/mobile

React Native end-user app for Personhood. Stores the user's W3C VC in the device's secure enclave (iOS Keychain / Android Keystore) and presents it on demand to integrator apps.

Status: deferred to v0.2. The web app at `../web/` is the v0.1 enrollment surface.
