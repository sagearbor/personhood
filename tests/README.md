# tests

End-to-end tests that exercise the full Personhood ceremony: method completion → credential issuance → policy evaluation → nullifier derivation. Unit tests live inside each Go module (e.g. `src/policy/*_test.go`).

Status: not yet populated. Will hold a `go test ./...` suite plus Playwright tests against the web app.
