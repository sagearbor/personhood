# src/registry

Method plugin registry. Methods register themselves here at startup; the issuer server and the verifier SDK query this registry to discover available methods, their metadata, and their strengths. See `INTERFACE.md` for the registry contract.
